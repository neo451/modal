s"[bd|~] east"
  |> d1

note scale "kumai", "0 1 2 3 4 5 7"
  |> apply s"superpiano", _
  |> jux rev, _
  -- |> apply vowel"<a e i o u>", _
  |> apply room"0.9", _
  |> apply delay"<0.1 0.2 0.4 0.8>", _
  |> degradeBy 0.2, _
  |> d2
